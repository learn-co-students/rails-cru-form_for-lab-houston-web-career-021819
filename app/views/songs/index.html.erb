<h1>Songs Index</h1>
    <ul>
        <%@songs.each do |song|%>
            <li><%= link_to song.name, song_path(song) %></li>
        <%end%>
    </ul>
<div><%= link_to 'Create New Song', new_song_path %></div>